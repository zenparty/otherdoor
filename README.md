# otherdoor

Static site built with [Eleventy](https://www.11ty.dev/).

- `npm run dev` — local server at http://localhost:8080
- `npm run build` — outputs to `_site/`
- Deploy: upload the contents of `_site/` to Porkbun static hosting via FTP (FileZilla)
